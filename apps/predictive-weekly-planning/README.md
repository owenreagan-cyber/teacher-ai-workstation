# Predictive Weekly Planning Workstation

Run `python3 scripts/canvas_llm_phase22/phase22_workstation.py serve --host 127.0.0.1 --port 8765` and open `http://127.0.0.1:8765`.

SQLite data lives in ignored `.local/canvas-llm/phase-22-predictive-weekly-planning/`. No Canvas writes or email sends exist.
