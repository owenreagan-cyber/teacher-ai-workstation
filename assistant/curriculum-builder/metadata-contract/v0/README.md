# Metadata Contract v0 (Inactive)

```text
Status: inactive planning artifacts only
Validator: none
Runtime: not active
Ingestion: blocked
```

These JSON files are **inactive schema samples** for Programs A4–A7. They are not validated by any CLI command and do not activate registry writes, ingestion, or generation.

See `docs/curriculum-builder-canonical-contract-schemas.md`.
