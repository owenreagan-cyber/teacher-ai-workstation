# A4–A7 Negative Fixture Reference (Invalid Examples)

```text
Classification: negative_blocked_fields_reference
Runtime: none — must fail A4–A7 cross-validation
```

These JSON files document **invalid** fixture patterns. They must **fail** validation when checked by `scripts/curriculum-builder-registry-a4-a7-fixture-schema-status.sh`.

Do not use as canonical fixtures. No real curriculum content, student data, or live integration fields.
