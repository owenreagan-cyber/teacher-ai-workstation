# Registry v0.2 Renderer Preview Input

Renderer preview reads the local fake fixture registry only.

- Text/markdown metadata preview only — no lesson generation
- See `scripts/curriculum-builder-registry-v0-2-render-preview.sh`
