# Curriculum Library Reference v0

Read-only planning fixtures for Curriculum Library v1 foundation.

- `library-reference-schema.json` — JSON Schema for fictional library reference records
- `sample-library-references.json` — canonical placeholder reference fixtures

This directory does not activate scanning, indexing, APIs, or source resolution.

Validate:

```bash
bin/chief-of-staff --curriculum-library-reference-v0-validate
```
