# Local Retrieval v0

Read-only lookup interface planning fixtures for Phase 3 Workstream D.

- `lookup-interface-schema.json` — JSON Schema for fictional manual lookup records
- `sample-lookup-fixtures.json` — canonical placeholder lookup and missing-reference fixtures

This directory does not activate retrieval engines or automatic resolution.

Validate:

```bash
bin/chief-of-staff --local-retrieval-foundation-v0-validate
```
