# Presentation Engine — Renderer Planning Fixtures

```text
Classification: fake_local_planning_only
Runtime: none
Schema activation: none
```

These JSON files are **planning-only examples**. They are not consumed by any renderer, exporter, or generator.

## Fixtures

| File | Purpose |
| --- | --- |
| `example-presentation-plan-001.json` | Fake presentation plan labels |
| `example-slide-outline-001.json` | Fake slide outline labels |
| `negative/blocked-runtime-export-example.json` | Documents forbidden fields (negative reference) |

## Rules

- Fake labels only (`example-math`, `fake-local-resource-label`, etc.)
- No real curriculum text, student data, URLs, or file paths
- No generation prompts or API fields
