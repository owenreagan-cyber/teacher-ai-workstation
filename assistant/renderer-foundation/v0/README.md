# Renderer Foundation v0

Read-only renderer interface planning fixtures for Phase 3 Workstream C.

- `renderer-interface-schema.json` — JSON Schema for fictional renderer interface records
- `sample-renderer-manifests.json` — canonical placeholder manifests (one per canonical contract type)

This directory does not implement renderers or generate outputs.

Validate:

```bash
bin/chief-of-staff --renderer-foundation-v0-validate
```
