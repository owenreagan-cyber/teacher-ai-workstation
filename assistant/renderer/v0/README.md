# Renderer Input Manifest v0

Read-only input readiness planning manifest for Renderer Foundation v1.

- `renderer-manifest.json` — required input sources and fail-safe rules for future renderers

Interface manifests live at `assistant/renderer-foundation/v0/sample-renderer-manifests.json`.

Validate:

```bash
bin/chief-of-staff --renderer-input-readiness-v0-validate
```
