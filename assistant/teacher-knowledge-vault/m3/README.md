# Teacher Knowledge Vault M3 — Fake Duplicate, Search, and Package Fixtures

Fictional duplicate detection, search, and resource package examples only. **No real paths, curriculum names, or student data.**

```bash
bin/chief-of-staff --teacher-knowledge-vault-m3-fake-duplicate-search-status
```

All fixtures: `classification: fake_local_planning_only`, `runtime_executed: false` or `search_runtime_executed: false`.

Placeholder URIs: `fake-catalog://`, `fake-local-staging://` — never real filesystem paths.
