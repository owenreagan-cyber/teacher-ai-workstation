# Teacher Knowledge Vault M4 — Smart Rename, Evidence, and Review Card Fixtures

Fictional smart rename suggestions only. **No real paths, curriculum names, or student data.**

```bash
bin/chief-of-staff --teacher-knowledge-vault-m4-smart-rename-status
```

All fixtures: `classification: fake_local_planning_only`, `runtime_executed: false`.

Placeholder URIs: `fake-local-staging://`, `fake-catalog://` only.
