# Teacher Knowledge Vault M6 — Extraction and OCR Fixtures

Fictional extraction/OCR approval examples only. **No real extraction, OCR, or API calls.**

```bash
bin/chief-of-staff --teacher-knowledge-vault-m6-extraction-ocr-approval-status
```

All fixtures: `classification: fake_local_planning_only`, `runtime_executed: false`.

Placeholder URIs only. Text fields use `fake_snippet_label` placeholders — not real extracted curriculum text.
