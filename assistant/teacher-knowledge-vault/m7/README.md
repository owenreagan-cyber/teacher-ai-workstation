# Teacher Knowledge Vault M7 — Connector Fixtures

Fictional read-only connector approval examples only. **No real connectors, OAuth, API, or network access.**

```bash
bin/chief-of-staff --teacher-knowledge-vault-m7-connector-approval-status
```

All fixtures: `classification: fake_local_planning_only`, `runtime_implemented: false`, `runtime_connected: false`.

Placeholder URIs only: `fake-drive://`, `fake-nas://`, `fake-canvas://`, `fake-manual://`
