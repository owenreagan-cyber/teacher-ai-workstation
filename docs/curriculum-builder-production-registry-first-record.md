# Production Registry First Governed Record

Last updated: 2026-07-03

```text
Status: first_record_complete
Classification: one manual metadata record — write tooling blocked
Proof: --curriculum-production-registry-first-record-status
Record ID: resource-math-lesson-108-presentation
```

## Non-Activation Statement

```text
This record does not authorize writer scripts or active --write.
It does not authorize a second record or batch import.
BLOCKED-NO-WRITES.sentinel remains intact — automated writes remain blocked.
It does not authorize real curriculum file access, source auto-resolution, or integrations.
```

## First Record

| Field | Value |
| --- | --- |
| Path | `assistant/curriculum-builder/registry/v0-2/production-registry.json` |
| `records` count | `1` |
| Approved ID | `resource-math-lesson-108-presentation` |
| Sentinel | intact |

## Pre-Write Baseline

Pre-write empty shell snapshot:

`assistant/curriculum-builder/registry/audit/snapshots/production-registry-20260703T042100Z-pre-write.json`

## Sentinel Dual State

The first manual production record **exists**, but `BLOCKED-NO-WRITES.sentinel` **remains intact**. Writer scripts, `--write`, batch import, and unapproved mutation paths remain blocked.

## Related Documents

| Document | Role |
| --- | --- |
| `docs/curriculum-builder-production-registry-empty-file.md` | Historical empty-shell milestone |
| `docs/curriculum-builder-production-registry-metadata-pilot-execution-plan.md` | Pilot protocol |
| `docs/curriculum-builder-production-registry-sentinel-semantics.md` | Sentinel choice A semantics |
| `docs/proposals/backlog/production-registry-write-mission.md` | Write mission backlog |

## Non-Activation

This document does not authorize write tooling or a second record.
