# Deployment Manifest Preview

The deployment manifest is a read-only preview of what would happen later in the Canvas transport phases.

It never performs transport behavior and never calls Canvas.

Supported operation states:

- `ready`
- `needs-review`
- `blocked`
- `unchanged`
- `omitted`

