# Canvas LLM Knowledge Architecture Fixtures

```text
Status: fake_local_schema_examples_only
Classification: Level 0 fake/local fixture evidence
Runtime activation: no
```

These fixtures demonstrate Phase 4 JSON object shapes only. They contain no real Canvas course data, no student data, no real curriculum, no official Canvas style, and no official “Mr. Reagan” voice sample.

The fixtures use:

- `source_class`: `fake_local_fixture`
- `evidence_level`: `level_0_fake_local_fixture`
- `verification_status`: `fake_local_example`
- `approval_status`: `fixture_approved`

They do not authorize Canvas API/OAuth/live reads, Canvas writes/publishing, real curriculum ingestion, RAG, embeddings, generation, or runtime JSON/SQLite database writes.
