# Canvas LLM Manual Evidence Packets

```text
Status: fake_local_redacted_packet_fixtures
Classification: fixture-only packet examples
Canvas API/OAuth/live reads: blocked
Student data: blocked
Real curriculum ingestion: blocked
Production writes: blocked
```

This folder contains committed fake/local/redacted packet fixtures for Phase 3 normalizer checks. The normalizer is intentionally limited to explicit packet directories under this folder and does not scan arbitrary manual evidence, Canvas exports, real curriculum folders, or user home folders.
