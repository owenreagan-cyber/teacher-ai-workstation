# Canvas LLM Phase 3 Negative Safety Fixtures

These fake/local fixtures intentionally contain blocked metadata or blocked marker text so `scripts/canvas-llm-phase-3-status.sh` can prove the normalizer rejects them.

They contain no real Canvas data and no real student data.
